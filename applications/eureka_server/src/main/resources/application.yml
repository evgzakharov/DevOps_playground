server:
  port: ${SERVER_PORT}

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL}

management:
  security:
    enabled: false